name: BDD Tests

on:
  push:
    branches: [livermore]
  pull_request:
    branches: [livermore]

jobs:
  bdd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          pip install -e "."
          pip install pytest pytest-bdd pytest-cov
      - name: Run BDD tests
        run: |
          python -m pytest tests/test_bdd_scenarios.py -v
