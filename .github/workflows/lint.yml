name: Lint

on:
  push:
    branches: [livermore]
  pull_request:
    branches: [livermore]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install ruff
        run: pip install ruff
      - name: Ruff check (tests)
        run: ruff check tests/
      - name: Ruff check (source - informational)
        continue-on-error: true
        run: ruff check tradingagents/ cli/
      - name: Ruff format check (tests)
        run: ruff format --check tests/
      - name: Ruff format check (source - informational)
        continue-on-error: true
        run: ruff format --check tradingagents/ cli/
